# Drag Timer 1.4.1 release checklist

## Release scope

| Item | Result | Evidence |
| --- | --- | --- |
| macOS 27 menu-bar drag | Pass | Owner confirmed the updated drag interaction on macOS 27; PR #16 merged with green CI |
| Click still opens the popover | Pass | PR #16 test plan and `DragTimer --self-test` held-click coverage |
| Version classification | Pass | User-visible bug fix on a public macOS release, so the release advances from 1.4.0 to 1.4.1 |
| Release notes | Pass | `docs/releases/v1.4.1.md` describes the input-path fix, compatibility, and installation |

## Local release candidate

| Field | Value |
| --- | --- |
| Artifact | `Drag-Timer-1.4.1-macos-universal.zip` |
| Source | Local `main` candidate containing merged PR #16 plus v1.4.1 release metadata |
| SHA-256 | `e0820df3527671a3b96c5f07e2cb933bf628b4d94ea17a1df0e2886baa298e2e` |
| Tester and date | Project owner and Cursor, 2026-09-16 |
| Apple Silicon environment | Pass — owner confirmed macOS 27 drag; local build, self-test, archive, and signature checks |
| Intel environment | Not tested on physical hardware; native slice and archive structure verified |

## Automated gates

| Check | Result | Evidence |
| --- | --- | --- |
| Tag and bundle version | Pass | `./Scripts/validate-release.sh v1.4.1` returned `1.4.1` |
| Release validator negative tests | Pass | `./Scripts/test-validate-release.sh` passed |
| Info.plist syntax | Pass | `plutil -lint Packaging/Info.plist` passed with build number `7` |
| Swift build | Pass | Full Command Line Tools release and debug builds passed |
| XCTest suite | Pass on CI | GitHub Actions `build-and-test` on PR #16 passed; local `swift test` cannot import XCTest with Command Line Tools only |
| Deterministic self-test | Pass | `DragTimer self-check passed` |
| Universal executable | Pass | `lipo` reported exactly `arm64 x86_64` |
| Ad-hoc signature | Pass | `codesign --verify --deep --strict` passed before and after archive extraction |
| Final ZIP checksum | Pass | Local candidate matched `SHA256SUMS.txt` |
| Clean extraction | Pass | Version, CPU slices, and signature were rechecked after extraction into a new temporary directory |
| Source formatting | Pass | `git diff --check` passed |

## Manual interaction coverage

| Check | Result | Evidence |
| --- | --- | --- |
| Drag from menu-bar icon on macOS 27 shows preview | Pass | Owner confirmed the updated drag interaction |
| Release of a drag creates a timer | Pass | Owner confirmed on macOS 27; PR #16 test plan |
| Stationary click opens popover after release | Pass | PR #16 test plan and self-check held-click coverage |
| Small pointer jitter stays a click | Pass | Self-check jitter coverage |
| Drag returning to origin stays a drag | Pass | Self-check origin-return coverage |

## Publication gate

The annotated `v1.4.1` tag may be pushed only after this checklist, release notes, version bump, and documentation are committed on `main`. The tag-triggered GitHub Actions workflow must independently repeat the build, tests, self-test, universal packaging, signature verification, clean extraction, and checksum gates before it publishes the GitHub release.
